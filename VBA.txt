VBA for Lloyds:

Function test1(a, b, c) As Doubletmp = a + b + ctest1 = tmpEnd FunctionFunction Area(X As Double, y As Double) As DoubleArea = X * yEnd FunctionFunction EulerSDE(Xt, dt, a, b) As DoubleDim Xt As Double, randnum As DoubleRandomize '<-- Seed the random number generator with 'randomizerandnum = Application.WorksheetFunction.Norm_S_Inv(Rnd())EulerSDE = Xt + a * dt + b * randnum * Sqr(dt) 'EulerSDE = Xtplus1End FunctionFunction CKLSDrift(X, alpha, beta)CKLSDrift = alpha + X * betaEnd FunctionFunction CKLSDiffusion(X, sigma, gamma)CKLSDiffusion = sigma * (Abs(X) ^ gamma)End FunctionFunction CKLSMLE(path, alpha, beta, sigma, gamma, dt)' SDE: dX = (alpha - beta X) + sigma X^gamma dWDim summ As Double, N As IntegerDim term0 As Double, term1 As Double, term2 As Double, term3 As DoubleN = Application.WorksheetFunction.CountA(path)mypi = Application.WorksheetFunction.Pi()summ = 0For i = 1 + 1 To N    term0 = 1 / (VBA.Sqr(2 * sigma ^ 2 * mypi * dt) * (Abs(path(i - 1)) ^ (gamma)))    term1 = 1 / ((2 * sigma ^ 2) * dt * (Abs(path(i - 1)) ^ (2 * gamma)))    term2 = (path(i) - (path(i - 1) * (1 + beta * dt) + alpha * dt)) ^ 2    summ = summ + Application.WorksheetFunction.Ln(term0) - (term2 * term1)Next iCKLSMLE = summEnd Function